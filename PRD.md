# PRD｜28 天 Vibe Coding 打卡闯关站

> Day 4 产出｜输入来源：`research.md`｜下一步：Day 5 技术设计

## 一、项目概述

- **一句话**：把 28 天的 Vibe Coding 打卡，做成一个闯关样式的网页——每天完成打卡即通过一关，28 天对应 28 关。
- **给谁用**：参加本期 Vibe Coding 打卡的同学（约 100 人，来自全国各地）。
- **本质**：仍然是打卡工具。闯关只是**呈现方式**，目的是让每天打卡比「一堆文字」更有趣。
- **形态**：一个部署在网上、需要联网打开的网站；**不需要注册登录**。打卡内容与进度只保存在你这台设备上，不上传服务器。
- **课程文档**：`https://ncntd4qb99gs.feishu.cn/docx/S07bdyvfLohL75xFVaBcKTpin1f`（每关对应的完整任务内容在这个文档里）

## 二、目标与非目标

### 本期目标（MVP，第 1 周）

| # | 目标 |
|---|---|
| G1 | 一个能同时看到 28 关的关卡列表，一眼看出自己走到哪 |
| G2 | 每天打卡 = 通过当天那一关 |
| G3 | 逐关进行：任何时刻只有「最早的未完成关卡」可以打卡，补关也按这个顺序，且一天可以连着补多关 |
| G4 | 打卡时留下内容：截图或文字，至少留一项 |
| G5 | 每关有标题和任务概述，并提供一个跳转课程文档的按钮，点开可看完整任务 |
| G6 | 进度与打卡内容保存在本机，不需要登录 |
| G7 | 打卡当天可以修改内容；过了当天（北京时间）不能再改 |

### 非目标（本期不做 + 为什么）

| 不做的功能 | 为什么不做 | 理由类型 |
|---|---|---|
| 用户注册 / 登录 | 要「记住每个人的账号」，只能靠数据库；数据库是第 23 天才讲的内容 | 能力依赖 |
| 地区组队 PK / 排行榜 | **主因**：分组、排行、跨用户查看都要后端。**次因**：100 人分到全国各地区，必然出现有的地区 0 人组不成队、有的十几人碾压别队；PK 一旦有胜负就有人不高兴，与「没有失败这回事」冲突 | 能力依赖（主）+ 设计冲突（次） |
| 装备、金币、打怪等游戏系统 | 这个产品是打卡工具，游戏只是它的外观。做成一套游戏规则会让 MVP 失控、也维护不起 | 设计冲突 |
| 真实数据持久化 / 云同步 | 同上，依赖后端与数据库 | 能力依赖 |
| 实时看别人的进度 | 跨用户可见必须联网、必须有账号 | 能力依赖 |
| 每关单独配置打卡要求（有的必须截图、有的必须写文字） | 要做一套「按关配置」的系统，本期先用统一规则 | 成本失控 |
| 把截图存到腾讯云存储桶 | 从网页直接传文件，必须把密钥放进网页里，等于公开；安全做法要后端发临时密钥（第 23 天） | 能力依赖 |

## 三、用户与场景

**用户**：本期打卡的同学，一个人用一台设备。

| 场景 | 他的操作 |
|---|---|
| 每天打卡 | 打开网站 → 看到自己到第几关 → 点当前那一关 → 看标题和任务概述 → 点「开始打卡」→ 上传截图或写一段话 → 点「完成打卡」→ 回列表，看到进度前进了一格 |
| 漏了几天后回来 | 网站**仍然停在最早漏掉的那一关**；把它补上，下一关才解锁。可以一天连着补多关 |
| 当天发现写错了 | 在当天结束前重新打开该关，改掉截图或文字 |
| 事后回看 | 翻到之前打过的关，看当时留下的截图和文字 |
| 好奇后面几关是什么 | 点还没解锁的关，能看到它的标题（但「开始打卡」按钮点不了） |

**前提假设**：

- 网站需要联网才能打开；打卡内容与进度只存在本机，不上传。
- 始终用同一台设备的同一个浏览器（不需要登录，所以换了设备或浏览器就看不到之前的记录）。

## 四、功能需求

| 编号 | 功能 | 行为说明 |
|---|---|---|
| F1 | 关卡列表 | 显示 28 个关卡，编号 1–28 全部可见；区分「已完成 / 当前可打卡 / 未解锁」三种样子 |
| F2 | 关卡详情 | 显示本关标题 + 任务概述 + 一个「开始打卡」按钮。**未解锁的关也能进入看标题，但按钮置灰、点不了** |
| F3 | 打卡页（本期唯一的写入入口） | 点「开始打卡」后进入，显示：本关任务概述 + 跳转课程文档的按钮（去看完整任务）+ 截图上传区 + 文字输入区 + 「完成打卡」按钮 |
| F4 | 完成打卡 | 截图和文字至少有一项时，「完成打卡」按钮才可点击；完成后记录完成时间，本关记为已完成，下一关解锁 |
| F5 | 进度保存 | 关卡状态与打卡内容保存在本机；重新打开网站后仍在 |
| F6 | 回看 | 已完成的关卡可以再次打开，查看当时留下的截图和文字 |
| F7 | 当天修改 | 在**完成打卡的当天**（北京时间）可以修改该关的截图和文字；跨过当天 24:00 后只能查看，不能再改 |
| F8 | 中途退出 | 没点「完成打卡」就关掉或刷新页面，已经填写的内容不保留，下次进入需重新填写 |

## 五、页面与流程

**三个页面**：

| 页面 | 内容 | 性质 |
|---|---|---|
| ① 关卡列表 | 28 个格子，已完成点亮、当前关高亮、后面的灰着 | 只读 |
| ② 关卡详情 | 本关标题 + 任务概述 + 「开始打卡」按钮（未解锁时置灰） | 展示页（含进入打卡的入口） |
| ③ 打卡页 | 任务概述 + 跳转课程文档按钮 + 上传截图 + 写文字 + 「完成打卡」 | **写入（唯一）** |

**主流程**：列表 → 点某一关 → 详情（看标题和概述）→ 点「开始打卡」→ 打卡页 → 填截图/文字 → 完成打卡 → 回列表（下一关解锁）

**要处理的异常路径**：

1. 点未解锁的关 → **可以进入、可以看到标题**，但「开始打卡」按钮是灰的、点不了。
2. 截图和文字都空 → 「完成打卡」不可点。
3. 用改地址的方式直接打开某一关 → 规则同上：能看标题，只有它是当前可打卡的那一关时按钮才可用。（具体地址形式由 Day 5 技术设计确定）
4. 过了完成当天再打开已完成的关 → 内容只读，不出现编辑入口。
5. 在打卡页填到一半就关掉或刷新 → 内容不保留。

## 六、状态与规则

**三种状态，以及它们各自的行为**：

| 状态 | 能否进入 | 看到什么 | 「开始打卡」按钮 |
|---|---|---|---|
| 已完成 | 可以 | 标题 + 当时留下的截图和文字 | 不出现 |
| 当前可打卡 | 可以 | 标题 + 任务概述 | 可点 |
| 未解锁 | 可以 | 标题 + 任务概述 | **置灰，点不了** |

> 「已完成」的关卡：在完成当天可以编辑已留下的内容，跨过当天后变成只读（见 F7）。

**核心规则一句话**：**任何时刻，只有编号最小的未完成关卡可以打卡。**

由此推出的规则：

- 完成打卡后，下一关**立即**解锁；同一天可以继续往下打，落后的人能连着补多关。
- **进度只增不减**：进度就是你打卡打出来的——没打卡，进度就不变，不会扣。
- 已完成的关卡随时可回看。
- **修改窗口**：只能在完成打卡的当天修改该关内容，以北京时间为准；跨过当天 24:00 后不可修改。
- 本期不支持**撤销打卡**（把已完成退回未完成）。
- 进度与打卡内容不在网络上传输，只存在这台设备上。

## 七、验收标准

| # | 验收标准 |
|---|---|
| 1 | 打开网站，28 个关卡全部可见，编号 1–28 无缺失 |
| 2 | 初次打开时，第 1 关的「开始打卡」按钮可点；第 2–28 关可以进入并看到标题，但按钮置灰、点不了 |
| 3 | 点第 1 关进入详情页，能看到本关标题、任务概述和「开始打卡」按钮 |
| 4 | 点「开始打卡」进入打卡页，能看到任务概述、跳转课程文档的按钮、上传区和文字输入区 |
| 5 | 点跳转按钮，能打开课程文档页面 |
| 6 | 截图和文字都为空时，「完成打卡」不可点击 |
| 7 | 只写文字、不传截图，可以成功完成打卡 |
| 8 | 只传截图、不写文字，可以成功完成打卡 |
| 9 | 完成打卡后回到列表，第 1 关显示为已完成，第 2 关的按钮变为可点 |
| 10 | 刷新网站后，第 1 关仍是已完成，第 2 关仍是当前可打卡的那一关（进度没丢） |
| 11 | 重新打开第 1 关，能看到当时留下的截图和文字 |
| 12 | 落后时（如已完成第 1、2 关、第 3 关未打），可打卡的是第 3 关；同一天可以连着补完第 3、4 关，每完成一关下一关立即解锁 |
| 13 | 用改地址的方式直接打开第 5 关（此时当前关是第 3 关），能看到标题和概述，但「开始打卡」按钮置灰、点不了 |
| 14 | 在完成打卡的当天重新打开该关，可以修改截图和文字，修改后内容生效 |
| 15 | 跨过完成当天后（北京时间 24:00 之后）再打开该关，内容只能查看，不能修改 |
| 16 | 在打卡页填了内容但没点「完成打卡」就刷新，重新进入时填写内容为空 |
| 17 | 28 关全部完成后，列表显示全部已完成的状态 |

## 八、风险与已知问题

| # | 风险 | 说明 |
|---|---|---|
| R1 | 截图只存在本机 | 换电脑、换浏览器、清浏览器数据，28 张截图就没了。没有云端备份，因为云存储要后端 |
| R2 | 「必须按顺序」是同类产品里最严格的设计 | 研究中两个万星项目都在刻意避免「卡住」体验；本产品的缓解措施是「允许按顺序补关、且可一天连补多关」，但落后的人仍要连着补好几天 |
| R3 | 无账号 = 无法跨设备 | 手机打的卡，电脑上看不到，反之亦然 |
| R4 | 本机存储容量可能不够放 28 张截图 | 属于技术实现问题，留到 Day 5 技术设计确认；此处只做登记 |
| R5 | 关卡标题与任务概述需要人工同步 | 内容来自课程文档，文档改了，网站不会自动跟着变 |
| R6 | 「北京时间」无法强制校验 | 打卡数据存在本机，时间只能读设备自身的设置，改设备时间可能绕过当天修改窗口。留到 Day 5 技术设计处理 |

## 九、后续清单（本期不做，以后可能做）

| # | 事项 | 前置条件 |
|---|---|---|
| N1 | 截图存到腾讯云存储桶 | 第 23 天之后（需要后端发临时密钥） |
| N2 | 账号系统 + 跨设备同步 | 同上 |
| N3 | 看到别人的进度（弱社交，不做排行榜） | 同上 |
| N4 | 每关单独配置打卡要求 | 视本期使用后的实际需要 |
| N5 | 跨天修改 / 撤销已打卡的内容 | 视实际需要 |
| N6 | 把每一天的内容单独提取进网站，跳转从飞书改为站内页面跳转 | 视实际需要 |
| N7 | 真正的游戏系统（等级、装备等） | 可能永远不做——与产品定位冲突 |
