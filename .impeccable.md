# Design Context

> 本文件由 `impeccable` skill 的上下文收集流程生成。
> 用途：给后续所有设计工作提供「为谁而做、该有什么感觉」的判断依据。
> 最后更新：2026-09-24（Day 8）

## Design Context

### Users

- **是谁**：参加本期 Vibe Coding 打卡的同学，约 100 人，来自全国各地。
- **使用情境**：一个人、一台设备、同一个浏览器。每天打卡时打开一次，漏几天后回来补一次，偶尔事后回看。
- **要完成的事**：打开网站 → 看自己到第几关 → 点当前那一关 → 看任务概述 → 上传截图或写一段话 → 完成打卡 → 看进度前进一格。
- **情绪目标**：**被鼓励，而不是被评判**。PRD 明确写了「没有失败这回事」——界面不制造挫败感，漏了几天回来也不该显得像做错了事。

### Brand Personality

三个词：**笃定、安静、被认真对待**。

- 不喧哗，不煽动，不喊口号。
- 把「每天写一笔」这件事本身当作有价值的行为来对待。
- 语气像一本被认真使用的本子，而不是一个催促你的闹钟。

### Aesthetic Direction

**方向：手账 / 纸张质感，但克制。**

- **基调**：暖白纸底、墨绿与赭石色、衬线数字。像一本被长期书写的打卡本。
- **不要**：游戏化的热闹（等级、金币、胜利音效式的视觉）、冷白科技感。
- **反参考（明确不做）**：
  - 紫蓝渐变、青紫霓虹（典型 AI 味）
  - 毛玻璃 / glassmorphism
  - 纯黑 `#000`、纯白 `#fff` 大面积使用
  - Inter / Roboto / Arial 等无个性默认字体
  - 圆角 + 通用投影的「默认卡片」堆叠
- **主题**：仅浅色。不做暗色模式（本期范围外）。
- **依赖约束**：**不引入任何外部字体文件**，全部走系统字体栈。
  理由：保持零外部请求，`file://` 直接打开也不会因字体加载失败而变形。

### Design Principles

1. **进度是主角，装饰是配角。** 28 关的进度是用户唯一想知道的事，所有视觉手段服务于让它一眼可读。
2. **已完成要「被看见」，未解锁不该「像废墟」。** 大量灰格子是改前最大的问题——未解锁状态应表达「还在前面等你」，而不是「这里什么都没有」。
3. **对比靠尺度，不靠特效。** 用字号、字重、留白拉开层次，而不是靠阴影、渐变、发光。
4. **克制的不动，动就要有理由。** 动效只用于表达状态变化（解锁、完成、进入），不用于炫技。
5. **诚实优先。** 骨架阶段没做的功能，界面上要老实标注，不用假数据撑场面。

## 本次改造范围（Day 8）

- **只改** `styles.css`。
- **不改** `app.js` 的 DOM 结构与类名契约——避免破坏已有功能与验证断言。
- 如有必要新增类名，仅限 `styles.css` 内部可独立生效、且不影响既有结构的纯装饰性补充。

---

## 修订记录

### 2026-09-25（Day 10）：移除「反参考」第 6 条「回弹 / 弹性缓动」

按 `AGENTS.md` 第八节，删改规则条款须先列三问、并经规则所有者明确同意。留痕如下：

| 问 | 答 |
|---|---|
| 动的是哪一条 | 上一节「反参考（明确不做）」的第 6 条 |
| 原文是什么 | `  - 回弹 / 弹性缓动` |
| 为什么必须改 | 本日要在闯关地图的 28 个关卡落点上实现「果冻」交互——选中态先变宽、再变高，带弹簧过冲，邻居逐格让位。该交互的核心机制就是弹性过冲，与本条直接冲突 |

**同意留痕**：规则所有者（项目作者）于 2026-09-25 明确选择「只删冲突那一条」，
并同时确认实现方式为「原生 JS 手写弹簧，零依赖、零外部请求」。

**删除范围**：仅此一条。本文件的 Users / Brand Personality / 5 条 Design Principles 一字未动。

**技术前提未变**：仍不引入任何框架与外部依赖（`TECH_DESIGN.md` 决策①、④），仍保持零外部请求。

> 注：顶部「最后更新：2026-09-24（Day 8）」指 Design Context 的**收集时间**，非文件修订时间，
> 故未改动；Day 10 的修订以本节为准。
